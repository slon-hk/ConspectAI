# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
httpx==0.27.0

# ── Database ───────────────────────────────────────────────────────────────────
asyncpg==0.29.0
# pgvector Python client for type registration with asyncpg
pgvector==0.3.2

# ── Auth ───────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── AI / Gemini ────────────────────────────────────────────────────────────────
google-generativeai==0.7.2
Pillow>=9.0.0

# ── RAG: document parsing ──────────────────────────────────────────────────────
pdfplumber==0.11.1           # text extraction from PDF
PyMuPDF==1.24.4              # image extraction from PDF (fitz)
python-docx==1.1.0           # DOCX support
youtube-transcript-api==0.6.2 # YouTube transcript

# ── RAG: utilities ─────────────────────────────────────────────────────────────
# No LangChain — we own the pipeline, no abstraction overhead

# ── Cache ──────────────────────────────────────────────────────────────────────
redis[asyncio]>=5.0.0
msgpack>=1.0.0

# ── Pydantic ───────────────────────────────────────────────────────────────────
pydantic==2.7.1
tiktoken==0.7.0